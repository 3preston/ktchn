import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ktchnonline',
  appName: 'ktchn',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
